import {createRouter,createWebHistory} from 'vue-router';import Home from '../pages/Home.vue';import Details from '../pages/Details.vue';import Login from '../pages/Login.vue';import {useAuth} from '../store/auth';const routes=[{path:'/login',component:Login},{path:'/',component:Home,meta:{requiresAuth:true}},{path:'/package/:id',component:Details,meta:{requiresAuth:true}}];const router=createRouter({history:createWebHistory(),routes});router.beforeEach((to,_,next)=>{const a=useAuth();a.loadUser();if(to.meta.requiresAuth&&!a.user)next('/login');else next();});export default router;